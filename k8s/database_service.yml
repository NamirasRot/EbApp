apiVersion: v1
kind: Service
metadata:
  name: database-service
  namespace: database-namespace
  labels:
    app: database-service
spec:
  selector:
    app: database-service
  ports:
  - protocol: TCP
    port: 1111
    targetPort: 5432
    name: database-port
  type: LoadBalancer